<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.id.fikridzakwan.somethingbig">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SomethingBig">
        <activity
            android:name=".view.splashscreen.SplashScreenActivity"
            android:configChanges="uiMode"
            android:exported="true"
            android:theme="@style/Theme.SomethingBig.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.detail.DetailMovieActivity"
            android:configChanges="uiMode"
            android:exported="true"
            android:theme="@style/Theme.SomethingBig.NoActionBar"/>
        <activity
            android:name=".view.main.MainActivity"
            android:configChanges="uiMode"
            android:exported="true"
            android:theme="@style/Theme.SomethingBig.NoActionBar">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>